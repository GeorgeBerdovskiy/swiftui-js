import { HStack, Text } from "./functions/elements";

let Home = [
    // SwiftUI elements
    HStack([Text("Home")]),
    Text("You're looking at a page written in SwiftUI."),
]

export default Home;