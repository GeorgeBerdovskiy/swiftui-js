import SwiftUI

struct Main: View {
    var body: some View {
        HStack {
            Spacer()
            
            VStack {
                Text("Hello...")
                Text("World!")
            }
            
            Spacer()
        }
    }
}
